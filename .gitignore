# Ignore installed dependencies because they are restored with npm install.
node_modules/

# Ignore generated build output because it is recreated by npm run build.
dist/

# Ignore local TypeScript cache files.
*.tsbuildinfo
